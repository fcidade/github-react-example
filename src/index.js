import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

/**
 * Draw our application on a HTML div tag.
 * @param {object} app - Application component.
 * @param {Element} element - Element where the application will be placed.
 */
ReactDOM.render(<App />, document.getElementById('root'));
